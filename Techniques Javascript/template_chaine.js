const nom = "Turing";
const prenom = "Alan";

const message1 = "Mon nom est " + nom + " et mon prénom est " + prenom;

const message2 = "Mon nom est ${nom} et mon prénom est ${prenom}";

const message3 = `Mon nom est ${nom} et mon prénom est ${prenom}`;

console.log(message1);
console.log(message2);
console.log(message3);
